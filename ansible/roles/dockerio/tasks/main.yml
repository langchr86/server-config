---

- name: Ensure old docker-compose is not installed
  become: true
  ansible.builtin.apt:
    name: docker-compose
    state: absent

- name: Install docker.io and dependencies
  become: true
  ansible.builtin.apt:
    name:
      - apt-transport-https
      - ca-certificates
      - curl
      - docker.io
      - docker-compose
